# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  create_env:
    runs-on: ubuntu-latest
    steps:
      - run: |
          touch .env
          echo VITE_FIREBASE_API_KEY=AIzaSyD0ysA2Hh0X5nx4donSALczNWzEivRo-bo >> .env
          echo VITE_FIREBASE_DB_URL=https://my-community-center-list-default-rtdb.firebaseio.com/ >> .env
          echo VITE_FIREBASE_PROJECT_ID=my-community-center-list >> .env
          echo VITE_FIREBASE_APP_ID=1:529435335529:web:399197eef17703487dbbf8 >> .env
          cat .env
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm install && echo '-----Install Done 👌-----' && npm run build-only && echo '-----Build Done 👌-----'
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_MY_COMMUNITY_CENTER_LIST }}
          channelId: live
          projectId: my-community-center-list
